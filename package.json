{
  "name": "tester",
  "version": "1.0.0",
  "description": "\"Proyecto para hacer testing\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open -b \"electron\" --e2e",
    "cypress:open2": "cypress run --config ",
    "cypress:open4": "cypress run --help ",
    "test-trata-objetado-OK": "cypress run --spec 'cypress/e2e/test/cawcnuevo.cy.js' --tag 'Test' --record"
  },
  "eslintConfig": {
    "env": {
      "cypress/globals": true
    },
    "extends":"Standard",
    "plugins": [
      "cypress"
    ]
  },
  "author": "\"Ricardo Roa\"",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.3.1",
    "eslint-plugin-cypress": "^2.15.1"
  }
}
